/**
 * essa calsse é para facilitar o log
 * 
 * recebe dois parametros , o label e o que vc quer apresentar
 * **/

let log = {
    pull: (label, obj) => {
        console.log(`------------- ${lable} -----------------------\n'`);
        console.log(obj);
        console.log('\n----------------------------------------------------------\n');
    }
}